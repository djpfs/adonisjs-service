// import test from 'japa'

// test.group('Producer', () => {
//   test('can produce to a topic', (assert) => {})
// })

// test.group('Consumer', () => {
//   test('can consume a topic', (assert) => {})
//   test('can consume multiple topics', (assert) => {})
// })
